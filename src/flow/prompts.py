"""compose 프롬프트 — 수정이 잦은 문구는 전부 이 파일에 (bench4 compose/prompt.py 이식).

LLM 은 **세 곳**이다: rephrase_query · select_clips · refine_end_bound/refine_start_bound
(경계 둘은 뼈대가 같아 프롬프트 한 쌍으로 본다). 출력은 JSON 금지 — 줄 단위 키: 값
형식 (Qwen 파싱 안정성 실측 관례).

VERIFY_SYSTEM·verify_user 는 **호출부가 없다** — 채점 노드(score_match)를 폐기했다
(2026-08-20: 후보 90% 이상에 일치도 3 이 나와 변별이 없었다). 되살릴 때를 위해
파서(plan.parse_verify)와 함께 남겨 둔 것이지 현재 그래프가 쓰는 프롬프트가 아니다.

bench4 와 다른 점:
- 어휘 문자열을 하드코딩하지 않고 flow.vocab 에서 렌더 (드리프트 해소 —
  단, 결과 문자열은 bench4 원문과 byte 등가여야 한다: 등가 테스트가 고정).
- VERIFY 출력의 사유를 "장면 N: 한 줄" 로 명시 (A2 수정 — 클립별 사유 파싱).
"""

from flow import vocab

# 어휘 — select_clips 가 아는 말의 전부 (판별불가는 발행에 없으므로 제외).
# 하드코딩하면 vision3 에 태그가 늘어도 프롬프트만 옛 어휘로 남는다 (보크 실측).
TAG_VOCAB = ", ".join(t for t in vocab.PLAY_TAGS if t != "판별불가")
LABEL_VOCAB = ", ".join(vocab.LABELS)
# 판세는 2026-08-23 상류 개편으로 라벨에서 갈라져 나온 축이다 (game_context).
# 어휘 줄에서 빼면 '역전 장면' 같은 질의가 어느 축으로도 안 걸린다.
CONTEXT_VOCAB = ", ".join(vocab.GAME_CONTEXTS)

PLAN_SYSTEM = f"""\
[시스템 역할 및 규칙]
당신은 야구 하이라이트 편성자입니다. 제공된 경기 인벤토리 데이터를 바탕으로 하이라이트 편성 명세와 선곡을 작성하세요.

1. 용어 정의:
   - 태그: {TAG_VOCAB}
   - 라벨: {LABEL_VOCAB}
   - 판세: {CONTEXT_VOCAB}
   - 관점: 이닝 "초" = 원정팀 공격 / 이닝 "말" = 홈팀 공격
   - 용어 번역: 질의의 표현을 표준 용어로 변환 (예: 더블플레이 → 병살, 데드볼 → 사구)
   - 기타정보: 장면 직전 타석 자막의 **OCR 원문**입니다. 형식은 중계마다 달라 정해진 틀이 없으나 대개 타순·타자·타율·볼카운트와 상대 투수(P.로 시작)가 함께 적힙니다 (예: "1.최원준 .355 2-2 P.레일러 5" = 1번 타자 최원준, 투수는 레일러). 선수 이름을 묻는 질의는 이 줄에서 찾되, **P. 뒤는 투수**이므로 타자로 읽지 않습니다. 판독 오류가 섞일 수 있어 태그·전광판과 어긋나면 그쪽을 믿습니다.

2. 선곡 및 검증 규칙:
   - 반드시 인벤토리에 존재하는 장면 번호만 선택하며, 없는 번호는 절대 지어내지 않습니다.
   - 조건에 맞는 장면이 없으면 선곡 목록을 비웁니다.
   - 팀 하이라이트 요청 시: 해당 팀 공격(득점·안타·홈런) 및 수비 성공(상대 삼진·병살 처리) 위주로 선택합니다.
   - 수비 성공(캐치, 호수비, 다이빙 등) 탐색 시: 태그가 '안타'인 장면은 제외합니다. (안타는 수비 실패 결과임. 수비 성공은 범타·호수비 태그에 위치함)
   - 검색증거(화면/해설)와 보드 사실(태그/라벨)이 충돌하면 보드 사실을 우선시하되, 검색증거가 질의 의도에 부합하면 태그 지정 범위를 넘어 선곡할 수 있습니다.

3. 경기 종료 장면 (일반 하이라이트 요청일 때만):
   - 질의가 특정 플레이·선수·상황을 지정하지 않은 **일반 하이라이트 요청**이면(예: "하이라이트 모음", "이닝별 하이라이트", "경기 요약") 경기를 끝내는 마지막 아웃 장면을 반드시 포함합니다. 하이라이트는 경기가 끝나는 장면으로 닫혀야 합니다.
   - 찾는 법: 인벤토리에서 **가장 늦은 이닝**(회차가 가장 크고, 같으면 '말'이 '초'보다 늦음)에 속한 장면들만 보고, 그중 '전광판'에 '이닝종료'가 있는 **마지막** 장면이 그것입니다.
   - '이닝종료'는 매 반이닝 끝마다 붙으므로 **가장 늦은 이닝 밖에서는 찾지 않습니다.** 그 이닝 안에 없으면 넣지 않습니다 — 중계가 경기 종료 전에 끝난 경우입니다. 억지로 다른 장면을 대신 넣지 않습니다.
   - 질의가 특정 대상을 지정하면(예: "홈런 모음", "김도영 타석", "역전 장면") 이 규칙을 적용하지 않습니다.

4. 분량 규칙:
   - **분량은 신경 쓰지 않습니다.** 질의에 목표 시간이 적혀 있어도 길이를 맞추려고 애쓰지 마세요.
   - 질의에 맞는 장면을 **넉넉히** 고르면 됩니다. 최종 분량은 이후 단계가 중요도 순으로 맞춥니다.

[출력 예시]
5,26,46,59\
"""


def plan_user(query: str, game_line: str, inventory: str, feedback: str = "") -> str:
    """select_clips 유저 프롬프트 — 경기 정보 + 장면 블록 + (피드백) + 질의.

    검색 증거는 별도 블록이 아니라 **인벤토리의 해당 장면 블록 안에** 들어간다
    (plan.render_inventory 가 병합) — 번호로만 이어져 조인이 모델 몫이던 문제 해소.

    **분량 산술은 모델에게 시키지 않는다** (2026-08-24). 예산은 요청 인자
    (ComposeRequest.budget_sec)로 받아 finish 가 rank 순으로 덜어낸다.

    한때 프롬프트에 배수 보정을 걸었다가 되돌린 자리다. 인벤토리의 '영상길이'는 장면
    길이인데 실제 클립은 cut 이 좁혀 훨씬 짧아(comp42: 모델이 901초=15.0분을 골랐고
    최종 513초=8.6분, 43% 손실) 그걸 배수로 메우려 했는데, **85장면 길이를 합산해
    목표를 맞추는 산술이 사고를 두 배 이상 늘렸다**: 규칙 없는 comp42 는 204.8초에
    완주했고, 2배는 480초 가드를 넘겨 폴백(comp43), 1.5배도 492초로 넘겼다(comp44).
    비싼 계산으로 틀린 숫자에 다가가는 구조였다.

    지금은 모델에게 "넉넉히 고르라"만 시키고 길이는 코드가 책임진다. 남은 개선은
    인벤토리에 **실제 컷 길이**를 싣는 것이다 — cut.clip 은 LLM 0콜 순수 계산이라
    선곡 전에 전 장면을 미리 돌릴 수 있고(v201 85장면 10ms) 표기 오차가 +76% → -14%
    로 줄어, 모델이 무엇을 고르는지 더 정확히 보게 된다.
    """
    fb = f"\n[이전 시도 피드백]\n{feedback}\n" if feedback else ""
    return f"""[경기 정보]
{game_line}

[인벤토리]

{inventory}
{fb}
[질의]
{query}"""


# ── expand — 질의 해석 (신설) ────────────────────────────
# 사용자 질의는 편성자의 언어("이닝별", "모음")인데 색인된 텍스트는 중계의 언어
# ("넘어갑니다", "몸을 던져")다. 어휘가 겹치지 않아 임베딩이 표류한다 — 실측 v201:
# "이닝별 최고의 공격/수비 장면"은 20건 전부 해설에 최고 유사도 0.58, 1위가 스튜디오
# 잡담. 반면 "다이빙 캐치 호수비"는 0.66 에 상위 7/8 이 화면 캡션이었다.
EXPAND_SYSTEM = f"""\
당신은 야구 하이라이트 검색어 설계자다. 사용자 질의를 **중계 화면·해설에 실제로 나올
법한 표현**으로 다시 쓰고, 함께 걸 메타 필터를 고른다.

[어휘 — 필터로 쓸 수 있는 말의 전부]
- 태그: {TAG_VOCAB}
- 라벨: {LABEL_VOCAB}
- 판세: {CONTEXT_VOCAB}

[RULES]
- 검색어는 2~4개. 서로 다른 각도여야 한다 (해설 콜 / 화면 묘사 / 선수 이름).
- 편성 용어("모음", "하이라이트", "이닝별", "베스트")는 검색어에 넣지 않는다 —
  색인된 텍스트에 그런 말은 없다.
- 필터는 질의가 특정 플레이를 가리킬 때만 고른다. 폭넓은 질의면 "없음".
  필터는 좁히는 힌트일 뿐이라 틀려도 검색이 죽지는 않는다.
- 선수 이름이 질의에 있으면 검색어에 그대로 남긴다 (자막·해설에 이름이 나온다).

[OUTPUT — 아래 형식 그대로, 다른 말 금지]
검색어: <쉼표로 2~4개>
필터: <태그·라벨 쉼표 나열, 없으면 "없음">\
"""


def expand_user(query: str) -> str:
    """expand 유저 프롬프트 — 질의 한 줄."""
    return f"[질의] {query}"


# 후보 줄이 '다음 투구' 자리를 넘어 끝날 때 그 줄 끝에 붙는 꼬리표.
# END_SYSTEM 의 규칙이 **이 문자열 그대로** 가리키므로 둘은 같이 고쳐야 한다.
AFTER_PITCH_TAIL = "   ← 다음 투구 이후"

# ── bounds — 시작·끝 통합 보정 (구 endfix) ────────────────
# 시작과 끝을 따로 물으면 서로를 모른다 — 시작을 25초 당기면 끝의 여유도 달라진다.
# 후보는 결정적으로 만들고 LLM 은 **고르기만** 한다 (검증기가 제시 목록 밖을 기각).
END_SYSTEM = """\
당신은 야구 하이라이트 클립의 끝 지점 검수자다. 한 플레이의 진행이 시간순으로
주어진다. 그 플레이를 가장 잘 마무리하는 자리를 고른다.

[읽는 법]
- [진행] 아래 줄들은 **시간순**이다. 줄마다 `시작 ~ 끝` 시각이 붙는다.
- `[장면-유형] 설명` 은 그 구간의 화면이고, `[해설] "…"` 는 그 구간에 나온 말이다. 둘은 같은 시간축에 나란히 놓여 있다.
- '장면 시작'·'투구'·'결과 전광판'은 이 플레이의 기준점이다. 결과 전광판은 전광판에 결과가 반영된 자리이고, 늦게 반영되는 경우가 있다.
- '다음 타석 투구'가 붙은 줄은 이미 다음 타자의 투구다. 이 플레이가 아니므로 그 앞에서 끊는다.
- '다음 투구)'는 전광판이 다음 투구를 검출한 자리다. 그 시각부터는 다음 타자의 타석이다.
- '현재 끝점)'은 지금 클립이 끊기는 자리다. 그보다 위를 고르면 클립이 짧아지고, 아래를 고르면 길어진다.

[고르는 법]
- 번호가 붙은 줄만 고를 수 있다. **고른 줄의 끝 시각이 클립의 끝이 된다** — 그 화면·그 말까지 클립에 들어간다.
- 결과가 확정되고 그 결과를 설명하는 해설이 끝나는 자리를 고른다.
- 해설이 문장 도중이면 이르다. 해설이 이미 다음 타석·다른 화제로 넘어갔으면 늦다.
- 관중석·응원 화면이 이어지거나 다음 타자가 타석에 들어서면 그 앞에서 끊는다.
- 줄 끝에 '← 다음 투구 이후' 가 붙은 줄은 이미 다음 타자의 타석이다. 절대 고르지 않는다.
- 남은 줄 중에서 결과를 설명하는 해설이 끝나는 자리를 고른다.
- 결과가 아직 안 나온 자리는 고르지 않는다. 스윙만 남고 결과가 잘린다.

[RULES]
- 반드시 제시된 번호 중 하나를 쓴다. 시각이나 다른 말을 쓰지 않는다.
- 지금 자리가 이미 가장 낫다면 "유지"를 고른다. 억지로 고르지 않는다.

[OUTPUT — 번호 하나 또는 "유지", 다른 말 금지]
2\
"""

START_SYSTEM = """\
당신은 야구 하이라이트 클립의 시작 지점 검수자다. 제시된 시작 후보 중에서 그 플레이의
투구가 시작되는 지점을 고른다.

[후보 읽는 법]
- 맨 위 '현재'는 지금 클립이 시작하는 지점과 그 시각의 화면·해설이다.
- 후보마다 그 시각의 화면과 해설이 함께 적혀 있다. 시각을 따로 맞출 필요 없이 그 자리에서 판단하면 된다.
- 후보는 현재보다 앞일 수도 뒤일 수도 있다. 앞이면 당기는 것이고 뒤면 미루는 것이다.

[고르는 법]
- 그 플레이의 투구가 시작되는 지점. 투수가 공을 던지기 직전 화면이 그 자리다.
- 후보의 화면·해설에 나온 타자가 현재 장면의 타자와 다른 사람이면 앞 타석이다. 버린다.
- 현재에서 가장 가까운 투구를 고른다. 멀수록 앞선 파울이나 다른 볼카운트의 투구다.
- 해설이 이미 그 플레이의 결과를 말하고 있으면 그 지점은 늦다. 투구는 그보다 앞이다.
- 현재가 이미 그 플레이의 투구 지점이면 "유지"다.

[RULES]
- 반드시 제시된 후보의 번호 중 하나를 쓴다. 시각이나 다른 말을 쓰지 않는다.
- 제시된 후보가 전부 부적절하면 "유지"를 고른다. 억지로 고르지 않는다.

[OUTPUT — 번호 하나 또는 "유지", 다른 말 금지]
2\
"""


def hms(sec: float) -> str:
    """초 → 'HH:MM:SS' — 프롬프트 표기 전용 (2026-08-24).

    모델에게 초 단위 정수를 보여 주면 경기 시각을 머리로 환산해야 한다(9950s 가 몇 회
    언제인지 감이 안 온다). 상류 DB 표기(t_scene_baseball.start_time 등)도 같은 모양이라
    사람이 눈으로 대조할 때도 이 쪽이 맞다. 소수는 버린다 — 표시용이고, 적용은 후보
    번호로 원래 값(소수 포함)을 되찾는다.
    """
    h, rem = divmod(int(sec), 3600)
    m, s = divmod(rem, 60)
    return f"{h:02d}:{m:02d}:{s:02d}"


def start_user(rows: list[dict]) -> str:
    """bounds.start_rows() 산출 → 시작 후보 프롬프트 (클립 1건 = 콜 1건).

    뼈대가 end_user 와 **완전히** 같다 (2026-08-24): 같은 머리줄, 같은 '현재/후보 +
    화면·해설' 묶음, 번호 붙은 후보, 같은 [질문] 한 줄. 두 노드가 하는 일이 "제시된
    지점 중 고르기"로 같은데 형식이 달라 규칙과 파서를 따로 들고 있었다.

    구간의 대사·화면 블록은 뺐다. 남겨 뒀던 근거는 "시작 후보는 그 초의 화면 분류가
    비어 있는 경우가 많다"였는데, 실측이 그 걱정을 지웠다 — 근거·수치는
    bounds.start_rows docstring. 블록이 없으면 각 후보 줄이 자기완결적이라 모델이
    시각을 맞춰 조인할 일이 없어진다.
    """
    r = rows[0]
    lines = [_head(r, "시작"), *_cand_lines("현재 시작", r["cs"], r["cur"])]
    for i, c in enumerate(r["cands"], 1):
        lines += _cand_lines("시작후보", c["sec"], c, note=c.get("why", ""), num=i)
    return "\n".join(lines) + "\n\n[질문] 시작을 어디로 할까?"


def end_user(rows: list[dict]) -> str:
    """bounds.end_rows() 산출 → 끝 프롬프트. **화면과 해설이 한 줄기**로 깔린다.

    2026-08-24 재설계(2차). 줄마다 `시작 ~ 끝` 시각이 붙고, 화면(`[장면-유형]`)과
    해설(`[해설]`)이 같은 시간축에 나란히 선다. 자를 수 있는 자리에만 번호가 붙어
    서사와 선택지가 같은 줄이다 — 모델이 시각을 맞춰 조인할 일이 없다(그 조인이
    사고를 태우던 형식이다, _ctx docstring).

    번호를 고르면 컷 끝은 **그 줄의 끝**이 된다(bounds.end_rows 의 ends[n-1]).
    화면 줄이면 그 화면까지, 해설 줄이면 그 말이 끝나는 자리까지다. 해설을 후보로
    올린 이유가 여기 있다 — 결과를 설명하는 문장은 화면 경계와 어긋나는 일이 흔한데,
    화면만 후보면 문장 도중에 끊거나 다음 화면을 통째로 무는 수밖에 없었다.

    화면 설명·해설은 **전문 그대로** 싣는다. 자르면 그 자리를 고를 근거가 함께 잘린다.
    """
    blocks = []
    for r in rows:
        # 머리는 장면 식별자만 — 지금 컷 구간은 아래 '현재)' 줄이 화면·해설과 함께
        # 보여 준다. 같은 걸 두 번 쓰면 줄기가 흐려진다 (시작 프롬프트는 후보 목록
        # 형식이라 _head 를 그대로 쓴다).
        head = (f"■ 장면 {r['scene_id']} [{','.join(r['tags'])}]"
                + (f" {r['inning']}" if r.get("inning") else ""))
        span = (f"[진행] 장면 {hms(r['scene_s'])}~{hms(r['scene_e'])}"
                f" ({r['scene_e'] - r['scene_s']:.0f}s)")
        lines = [head, span]
        for ln in r["lines"]:
            lines += _line(ln)
        blocks.append("\n".join(lines))
    return "\n\n".join(blocks) + "\n\n[질문] 끝을 어디로 할까?"


def _line(ln: dict) -> list[str]:
    """줄 1개 — 화면(seg)·해설(utt)·다음 투구(pitch)·현재 끝점(cur).

    화면과 해설은 `시작 ~ 끝` 시각을 단다. 시각을 구간으로 주는 건 고르는 값이 그
    줄의 **끝**이기 때문이다. 시작만 보이면 "이 해설이 어디서 끝나는지"를 모델이 알
    수 없어 다음 줄의 시작으로 어림잡는다.

    '다음 투구'와 '현재 끝점'은 시각 하나다 — 구간이 아니라 **자리**라서. 둘 다
    번호가 없어 고를 수 없고, 문장이 아니라 표지로 읽히도록 `…)` 꼴을 맞춘다.

    `after_pitch` 가 붙은 후보는 줄 **끝에** 꼬리표를 단다. 표지 줄은 시간순 제 자리
    (그 검출 초)에 서는데, 줄이 **시작** 시각 순이라 그 표지가 정작 문제의 후보보다
    아래에 찍힌다 — 근거·실측은 bounds._end_lines. 그래서 판단이 일어나는 그 줄에
    같은 사실을 한 번 더 놓는다.
    """
    if ln["kind"] == "cur":
        return [f"  현재 끝점) {hms(ln['s'])}"]
    if ln["kind"] == "pitch":
        return [f"  다음 투구) {hms(ln['s'])}"]
    head = f"  {ln['num']:>2})" if ln.get("num") else "      "
    span = f"{hms(ln['s'])} ~ {hms(ln['e'])}"
    tail = AFTER_PITCH_TAIL if ln.get("after_pitch") else ""
    if ln["kind"] == "utt":
        return [f'{head} {span} [해설] "{ln["text"]}"{tail}']
    mark = f"{' · '.join(ln['marks'])} " if ln.get("marks") else ""
    return [f"{head} {mark}{span} [장면-{ln.get('shot_type') or '미분류'}]"
            + (f" {ln['shot']}" if ln.get("shot") else "") + tail]


def _head(r: dict, _kind: str = "끝") -> str:
    """행 머리 — 장면 번호·태그·이닝·현재 구간. 끝·시작 프롬프트 공용."""
    return (f"■ 장면 {r['scene_id']} [{','.join(r['tags'])}]"
            + (f" {r['inning']}" if r.get("inning") else "")
            + f" — 현재 {hms(r['cs'])}~{hms(r['ce'])} ({r['ce'] - r['cs']:.0f}s)")


def _cand_lines(label: str, sec: float, ctx: dict, note: str = "",
                num: int | None = None) -> list[str]:
    """후보 1건 = 헤더 + 화면 + 해설. 값이 없는 줄은 아예 내지 않는다(빈 줄이 늘면
    프롬프트만 길어지고 읽기가 나빠진다). num 이 있으면 앞에 '1)' 처럼 번호를 단다."""
    head = f"  {label} {hms(sec)}" if num is None else f"  {num}) {label} {hms(sec)}"
    out = [head + (f"  {note}" if note else "")]
    if ctx.get("shot") or ctx.get("shot_type"):
        out.append(f"     화면 [{ctx.get('shot_type') or '미분류'}] "
                   f"{ctx.get('shot') or '(설명 없음)'}")
    if ctx.get("utt"):
        out.append(f"     해설 \"{ctx['utt']}\"")
    return out


# ── verify — 클립별 채점 (기각권 없음) ──────────────────────
# 등급을 LLM 재량에 맡기면 매번 기준이 흔들려 A/B 비교가 안 된다. 정도 부사가 아니라
# **관찰 가능한 조건**으로 앵커를 박는다.
VERIFY_SYSTEM = """\
당신은 야구 하이라이트 검수자다. 클립을 지우거나 순서를 바꾸지 않고 채점만 수행한다.

[판정 우선순위]
클립 라벨 > 전광판 > 화면/해설 흐름
(라벨과 해설이 충돌하면 실제 발생한 플레이 화면/해설을 기준 처리)

[채점 기준]
1. 일치도 (0~3)
- 3: 질의 내용과 직접 일치 (화면/해설 명시)
- 2: 맥락상 맞으나 근거가 약함
- 1: 질의와 직접 관련 없으나 같은 이닝 흐름상 유용
- 0: 무관

2. 완결성 (정상/문제)
- 정상: 투구/타격 시작부터 플레이 결과(득점/아웃/리액션)까지 포함
- 문제: 첫 샷이 이전 상황 잔상이거나, 마지막 샷에 결과가 없음

[사고 절차 (Thinking 가이드)]
1단계: 첫 샷(시작)과 마지막 샷(결과) 확인 -> 완결성 결정
2단계: 플레이 내용 확인 후 질의와 대조 -> 일치도 결정
3단계: 출력 형식에 맞춰 한 줄 작성

[OUTPUT FORMAT - 오직 이 형식으로 한 줄만 출력]
장면 <번호>: <일치도> <정상|문제> <한 줄 근거>\
"""


def verify_user(query: str, spec_line: str, packet: str) -> str:
    """verify 유저 프롬프트 — 질의 + 명세 한 줄 + 클립 패킷 1건.

    질의가 없던 게 결함이었다: 시스템은 "질의에 얼마나 맞는지 채점한다"로 시작하는데
    유저 쪽엔 명세(모드·대상·관점·예산)만 실려, 모델이 대상 태그 목록에서 질의를
    역추정해야 했다 (2026-08-20 발견).
    """
    return f"""[질의] {query}
[명세] {spec_line}

{packet}

[질문] 이 클립의 일치도와 완결성은?"""
